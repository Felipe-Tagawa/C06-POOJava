public class Main {
    public static void main(String[] args) {
        // Conjunto de métodos públicos -- acesso externo(clientes)
        // Contém especificações para outras classes(manual de implementação)
        // Diferença com a Classe Abstrata: interfaces só abstraem comportamentos(métodos), e não atributos.
        // Importante em casos que precisaríamos de uma herança múltipla.
        // Setas pontilhadas(ou <<interface>>).
    }
}